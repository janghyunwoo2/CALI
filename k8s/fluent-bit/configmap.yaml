# =====================================================
# Fluent Bit ConfigMap
# =====================================================
# 설명: Fluent Bit 설정 파일을 ConfigMap으로 관리
# =====================================================

apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-config
  namespace: cali-system
data:
  fluent-bit.conf: |
    # TODO: apps/fluent-bit/fluent-bit.conf 내용 복사
  parsers.conf: |
    # TODO: apps/fluent-bit/parsers.conf 내용 복사
