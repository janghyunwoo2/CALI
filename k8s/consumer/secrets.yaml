# =====================================================
# Consumer Secrets (외부 시크릿 참조)
# =====================================================
# 설명: AWS Secrets Manager와 연동 (External Secrets Operator 사용 시)
# 또는 수동으로 Secret 생성
# =====================================================

# TODO: External Secrets Operator 설치 시 ExternalSecret 리소스 작성
# 또는 아래와 같이 수동 Secret 생성

apiVersion: v1
kind: Secret
metadata:
  name: cali-secrets
  namespace: cali-system
type: Opaque
stringData:
  openai-api-key: "sk-your-api-key"  # TODO: 실제 값으로 변경 또는 Secrets Manager 연동
  slack-webhook-url: "https://hooks.slack.com/..."  # TODO: 실제 값으로 변경
